$if(it.url)$\href{$it.url$}{$it.name.literal$}$else$$it.name.literal$$endif$\footnote{$if(it.attributes.corresponding)$Corresponding author: $endif$$for(it.affiliations/first)$$affiliation.tex()$$endfor$$if(it.email)$ E-mail: \texttt{$it.email$}$endif$}$if(it.orcid)$\,~\orcidlink{$it.orcid$}$endif$
